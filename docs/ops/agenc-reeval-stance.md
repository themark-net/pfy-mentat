# AgenC re-evaluation stance (T-0046)

**ADR-0010** remains in force: AgenC is **not** primary runtime.

## Re-open only if

Upstream closes the gaps that drove demotion (TUI approval UX, Grok-equivalent path, marketplace-style jobs usable as coding operator). Then:

1. Side-by-side vs Grok CLI + agent-cage  
2. New ADR if promotion considered  
3. No silent catalog tier jump to S without lab receipts  

## Current action

**No re-eval this cycle** — document stance only. Catalog stays B/watch.

# Eval pipeline (MVP + v0.2)

See [examples/eval-harness/](../../examples/eval-harness/).

```bash
make eval-mvp      # tier0 + one task
make eval-v02      # tier0 + all tasks (gate model)
make eval-matrix   # tasks × models → results.latest.md
```
